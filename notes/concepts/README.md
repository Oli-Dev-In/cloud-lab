# Concepts

Technical concepts explained in my own words.

One file per concept. Each file answers three questions:
- What is it?
- Why does it exist — what problem does it solve?
- How does it work in practice?

## Index

| File | Concept |
|---|---|
| `linux-filesystem.md` | Linux filesystem structure, file types, key directories, the prompt |
| `linux-kernel.md` | Linux kernel, kernel space vs user space, system calls, monolithic vs modular |
| `linux-boot-sequence.md` | BIOS POST, GRUB2, kernel initialization, systemd, runlevels/targets |
| `bash-environment.md` | Shells, variables, $PATH, $HOME, ~/.profile, PS1, built-in vs external commands |
| `virtualization.md` | Virtualization, hypervisors Type 1 and Type 2, Host vs Guest |
| `iso-disk-image.md` | ISO disk image, use in VMs and AWS AMIs |
| `ssh.md` | SSH protocol, client/server model, reconnection procedure |
| `port-forwarding.md` | Port forwarding, NAT, VirtualBox configuration |